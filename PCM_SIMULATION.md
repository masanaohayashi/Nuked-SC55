# PCM チップ: エミュレーションからシミュレーションへ

対象: `src/backend/pcm_sim.{h,cpp}`（新規）、検証台 `tools/sc55abcompare.cpp`（新規）
比較対象: `src/backend/pcm.cpp`（既存のサイクル単位エミュレーション）

---

## 0. いまどこまで来たか

ボイス経路の**内部状態がチップと一致した**。同じレジスタ列を与えて並走させた結果:

| 比較対象（チップ側のレジスタ） | 誤差 |
|---|---|
| 読み出しアドレス `ram1[4]` | **完全一致 100.00 %** |
| サブ位相 `ram2[8]` 下位14bit | **完全一致 100.00 %** |
| 差分デコードの走行和 `ram1[5]` | **0.0 %** |
| フィルタ low `ram1[3]` | 0.1 〜 0.9 % |
| フィルタ band `ram1[1]` | 0.4 〜 1.8 % |
| TVA エンベロープ `ram2[9]` | 0.0 % |
| 第2エンベロープ `ram2[10]` | 0.2 〜 2.3 % |
| カットオフ `ram2[11]` | 0.2 〜 1.1 % |

残る 0.1〜2 % は、20bit の中で 1LSB を守るために入っていた丸め（`addclip20(a, x>>1, x&1)` のキャリー入力）を意図的に落としたぶん。float32 の仮数は 24bit あるので不要と判断した。

**未達**: 出力段（ミキサ）の比較。ここはまだ相関 0.48〜0.96 で曲によってばらつく。原因は下記 §4。

---

## 1. 何を「機構」として捨て、何を「振る舞い」として残したか

| 元の姿 | 判定 | シミュレーション側 |
|---|---|---|
| 4段アンロールされたアドレスパイプライン | **振る舞い** | 4段のまま。元々これは分岐を消すための展開で、SIMD 化にそのまま乗る |
| 20bit 飽和加算 `addclip20` | **振る舞い（必須）** | `clip20()` = `fmin(fmax(...))`。10箇所。§2 参照 |
| `addclip20` のキャリー入力による丸め | 機構 | 捨てた。float32 の仮数で足りる |
| ブロックフロート指数の入れ子シフト | 振る舞い | `delta * g_block_scale[n]`、16エントリの表 |
| `calc_tv` の `tv_counter` ビット切り出し | 機構 | `speed` から引く256エントリの定数表3本に畳んだ |
| 同 ディザ `addlow` | 機構 | 捨てた。float の小数部が同じ役割をする |
| `multi()` による8bit符号付き乗算の連鎖 | 振る舞い | Chamberlin SVF。§3 参照 |
| 固定小数の粗/微2段ゲイン | 振る舞い | `level * (1/16384)` の線形ゲイン |
| ノイズシェーピング出力段 | **未着手** | §4 |

---

## 2. 飽和は捨てられない

元コードのボイス経路にある `addclip20` を全部列挙して突き合わせた。飽和が必要な理由は3つあり、どれも「固定小数の名残」ではない:

1. **ミックスバス** — 1ボイス足すごとに飽和する（順序依存）。SC-55 が音を重ねると歪むのはこれ
2. **フィルタ状態** — SVF は Q を上げると自励発振する。20bit で頭打ちになるから止まっている。float で外すと発散して NaN になる
3. **DPCM の走行和** — 差分の積分なので、ループ点で基準がずれると際限なく育つ

加えて**ゲイン2段の段間**（元 1458/1463 行）。ゲインは1段最大 2.0、2段で 4.0 なので、畳むとレールに張り付いた信号が素通りする。

---

## 3. フィルタは Chamberlin 型の状態変数フィルタ

演算を追うとそのままこれだった:

```
low  += F * band
high  = in - low - Q * band
band += F * high
```

`F` は `ram2[11]`（カットオフ、粗 `/64` + 微 `/8192` の2ワード）、`Q` は `ram2[6]` bit8–14 を `/64`。
出力タップは `ram2[6]` bit1 で low / high を選ぶ。

**マルチモードであることは実測で確認した**（4曲、発音中ボイスをサンプリング）:

| 曲 | low | high | high かつカットオフ全開 |
|---|---|---|---|
| TOKMEDLY | 94.1 % | 5.9 % | 0.0 % |
| IMAGA_55 | 82.4 % | 17.6 % | 0.0 % |
| GATCHA55 | 85.5 % | 14.5 % | 0.0 % |
| 55ZABRGR | 74.9 % | 25.1 % | 0.0 % |

high タップは 6〜25 % 使われている。F=0 なら `high = in` になるのでバイパス実装の可能性を疑ったが、
「カットオフ全開かつ high」が全曲 0.0 % なので、バイパスではなく本当の第2応答として使われている。

副産物: **カットオフは一度も全開にならない**（`ram2[11] >= 0x7f00` が 0.0 %）。
レゾナンスは 83 % が一つの値に集中しており、GS のレゾナンス初期値 64 と一致する。

---

## 4. 未解決: 出力段はノイズシェーパ

```c
accum_l += ram1[30][0];                          // 前回の残り下位ビットを戻す
ram1[30][2] = accum_l + (orval | noise);
ram1[30][0] = accum_l & write_mask;              // 下位ビットを保持
samp_l = (ram1[30][2] & ~write_mask) << 12;      // 上位ビットだけ出力
MCU_PostSample(...)
// これがもう一度繰り返される
```

つまり **ΔΣ 変調**であり、しかも**ボイス1巡につき2回出力する**（ミキサはボイス更新の2倍レートで動く）。
個々の出力サンプルは信号そのものではないので、シミュレーションの素の値と1対1で比べても相関は出ない。

現状は「2ポストを平均して、両者を1極で平滑してから比較」しているが、これは近似にすぎない。
帰還を含めた出力段をモデル化しないと、ミキサ以降は正しく評価できない。

参考値（平滑 0.005、8秒）: IMAGA_55 0.963 / TOKMEDLY 0.836 / GATCHA55 0.567。

---

## 5. 途中で潰したバグ（同種の再発防止のため記録）

1. **補間の二重計上** — `test` はフレーム開始時の基準値＋**これから読む**差分3つ。積算後＋消費済みではない
2. **ブロック指数のラップ** — `shift = (10 - n) & 15` は n≥11 で折り返し、値が増えるどころか 1/32 になる
3. **パン／送りのスケール** — `>>6` は ÷64。÷128 ではない
4. **位相を float にしてはいけない** — DPCM は走行積分なので、位相の切り上がりが1回ずれると差分1個ぶんが永久に残る。位相は信号ではなくインデックス
5. **ループ判定は現在位置に対して行う** — 境界にいるフレームは進まない。ワンショットは `loop` へ飛ぶだけ、ピンポンは留まって方向だけ反転
6. **方向ビットは `sub_phase_of` に対応する段の値を採る** — かつ4段目では更新されない。ここを間違えるとピンポン素材だけが同期を失う（ワンショットは無傷なので気づきにくい）
7. **出力は1巡あたり2フレーム** — シミュレーションを1巡1回で回さないと倍速で走る
8. **`!active` のパスは `ram2[8]` を全消しする** — キーオンのパス自体が `!active` なので、ノートは1巡遅れて綺麗な位相から始まる

---

## 6. 次にやること

1. 出力段（ΔΣ + 2倍レート）をモデル化して、ミキサ以降を正しく比較できるようにする
2. エフェクト（slot 28–31、ERAM のブロックフロート）— 未着手
3. `PCM_Update` と差し替えて実測（現状の内訳では DSP が全体の約 40 %）
4. SIMD 化。SoA で書いてあり内側に分岐がないので、ROM 読み（gather）以外はそのまま乗る

---

## 付録: 検証台の使い方

```sh
./tools/sc55abcompare song.mid 8          # 曲を流して比較
./tools/sc55abcompare --notes 4 "" 4      # 和音を保持して比較
SC55_TRACE=1 ./tools/sc55abcompare ...    # slot 0 の生値をフレーム単位で追う
SC55_SMOOTH=0.02 ./tools/sc55abcompare ...# 出力比較の平滑を変える
```

ビルドは `pcm.cpp` を `-DPCM_Write=PCM_Write_Impl`、`mcu.cpp` を
`-DMCU_PostSample=MCU_PostSample_Impl` でコンパイルしてフックする。手順は
`FIRMWARE_CONTROL_LOOP.md` の付録と同じ要領。

---

## エフェクト（ERAM のリバーブ／コーラス）— 構成の地図

ボイス側は浮動小数 + NEON に置き換わったが、エフェクトは回路転写の整数のまま残っている。
プロファイラの自己時間で `PCM_Update` が単独最大（約 27%）で、その中身がここ。
浮動小数化に着手する前の下調べとして、構成を書き出しておく。

### 遅延メモリ

**16,384 語ひとつの循環バッファ**（`eram[0x4000]`、`tv_counter &= 0x3fff`）。
32 kHz で回るので全長 **512 ms**。個別の遅延線があるのではなく、
**ひとつのバッファに対する読み書きのタップ位置**が `ram2[28][*]` と `ram2[29][*]` に入る。

したがって **ある段の遅延 = 読みタップ − 書きタップ**（バッファ長で剰余）。
タップ単体の値は遅延時間ではない。ここは読み違えやすい。

実測したタップ位置（起動 + 5 秒後）:

```
ram2[28][ 1]    926      ram2[29][ 0]   8102      ram2[29][ 6]  12729
ram2[28][ 2]   1307      ram2[29][ 1]   8962      ram2[29][ 7]  14079
ram2[28][ 3]   1419      ram2[29][ 2]   9580      ram2[29][ 8]  14334
ram2[28][ 4]   1420      ram2[29][ 3]  10965      ram2[29][ 9]  14336
ram2[28][ 5]   2194      ram2[29][ 4]  11436      ram2[29][10]  14966
ram2[28][ 6]   2750      ram2[29][ 5]  12345      ram2[29][11]  14868
ram2[28][ 7]   3997
ram2[28][ 8]   4421
ram2[28][ 9]   5371
ram2[28][10]   6053
ram2[28][11]   7583
```

読みタップ 22 本、書きタップ 13 本、段は 26 個。

### 段の種類

段 1〜4 は**シュレーダー型オールパス**の縦続（拡散段）:

```
s  = 遅延読み出し
v3 = 入力 − s            （v2 ^ 0xfffff, 1 が 2 の補数の引き算）
出力 = v3 * 係数 + s
```

段 5・6 は**一次ローパス**（減衰）:

```
y = y * a + 遅延読み出し * b
```

段 7 が左右に振り分け、段 8〜11 がさらにオールパス／コムを重ねて尾を作る。

係数は `ram2[30][*]` と `ram2[31][*]` に、上位バイト・下位バイトの組で入っている
（`multi(x, (int8_t)(v1 >> 8))` と `multi(x, (int8_t)(v1 & 255))`）。
`(v1 & 0x30)` が段の有効・無効。

### 浮動小数にすると消えるもの

`eram_pack` / `eram_unpack` はブロック浮動小数の詰め替えで、これは 16 ビットの
遅延メモリに 20 ビットの値を押し込むための仕掛け。**浮動小数にした時点で丸ごと不要**に
なる。ボイス側でブロック浮動小数の波形展開が消えたのと同じ事情。

`addclip20` の飽和は残す必要がある（フィードバック経路の安定に効く）。

### 動いているかの確認（と、危うく誤読しかけた話）

エフェクトの状態を覗こうとして `pcm.ram1[28][*]` / `ram1[29][*]` を標本化したところ、
20 秒のあいだ一度も 0 以外にならなかった。さらに `eram` も 16,384 語すべてゼロで、
`eram_pack` は 1,400 万回呼ばれて書いた値が全部ゼロだった。

**「エフェクトが無音を処理し続けている」と結論しかけた。** 出す前に音で確かめたのが
効いた。単音を鳴らして消音後の尾を測ると:

```
リバーブ送り 127:  250 ms 後 -39.8 dB、350 ms 後 -41.2 dB
リバーブ送り   0:  250 ms 後 -47.7 dB、350 ms 後 -54.3 dB
```

**13 dB 差。残響は正しく動いている。** 遅延メモリの占有も送り量に素直に応じる:

```
送り 127 → 非ゼロ 14,225 / 16,384 語
送り  40 → 非ゼロ 14,030 / 16,384 語
送り   0 → 非ゼロ      0 / 16,384 語
```

つまり試験に使っていた 3 曲がリバーブを送っていなかっただけで、エミュレータの
問題ではなかった。**「動いていない」と「入力がゼロ」は別物**で、状態を覗くだけでは
区別できない。

もう一つ引っかかった点: `ram1[28..29]` は 1 パス内の**作業レジスタ**で、パスの合間に
覗くと常に 0 に見える。持続する状態は `eram` のほうにある。

ただし **コストの話は変わらない**。回路転写なので、送りがゼロで何も生まないときも
同じ量の計算をする（ボイス側で「0 音でも 24 音でも同じコスト」だったのと同じ）。
浮動小数版では、入力が尾の長さより長く無音なら丸ごと飛ばせる。

## エフェクトを条件を決めて測る

曲を借りてくると、リバーブの種類もパラメータも曲任せになって条件が固定できない。
`tools/sc55mkmidi.py` が GS の SysEx で明示的に設定した短い .mid を作り、
`tools/sc55revtest` がそれを鳴らしてタップ位置・係数・尾を並べる。

### 種類ごとに構成が別物

`40 01 30`（リバーブマクロ）を 0〜7 で振ると、タップの組がはっきり変わる:

| | 尾 -20 dB | ram2[28] のタップ（先頭 8） |
|---|---|---|
| Room1 | 296 ms | 0 323 562 705 706 953 705 705 |
| Room2 | 330 ms | 0 917 1295 1406 1483 2149 1424 1424 |
| Room3 | 448 ms | 0 917 1295 1406 1483 2149 1424 1424 |
| Hall1 | 612 ms | 0 917 1295 1406 1483 2149 2627 3699 |
| Hall2 | 448 ms | 0 926 1307 1419 1420 2194 2750 3997 |
| Plate | 342 ms | 0 323 562 705 706 953 1412 1091 |
| Delay | 339 ms | 0 **1 3 5 7 9** 3606 3606 |
| PanDelay | 518 ms | 0 **1 3 5 7 9** 3606 **7190** |

Room2 と Room3 はタップが同じで係数だけ違う。**Delay と Panning Delay は拡散段の
タップが 1,3,5,7,9 に潰れていて**（＝実質的に拡散しない）、遅延だけが 3606 と 7190 —
32 kHz で **112.7 ms とその倍の 224.7 ms**。Panning Delay が左右で別のタップを使うのが、
そのまま左右に振れる仕組みになっている。

**構成の読みがこれで裏付けられた。** 段 1〜4 を拡散段と読んでいたが、Delay 系で
そこだけ潰されるのは、拡散段だからこそ。

### パラメータとレジスタの対応

**リバーブレベル**（`40 01 33`）→ `ram2[30][2]` と `ram2[30][3]` の上位バイト:

```
level   0 → 0x0000 / 0x0000
level  64 → 0x4000 / 0x4000
level 127 → 0x7f00 / 0x7f00
```

段 17・18 が出力へ送るときの係数そのもの。**level がそのまま上位バイトに入る。**

**リバーブタイム**（`40 01 34`）→ `ram2[30][0]` の上位バイトと `ram2[30][9]`:

```
time   0 → 30[0]=0x00ba  30[9]=0x0000
time  32 → 0x38ba        0x1c00
time  64 → 0x71ba        0x3880
time  96 → 0xaaba        0x5500
time 127 → 0xbfba        0x5f80
```

**`30[9] = （30[0] の上位バイト） << 7`** が 4 点すべてで成立する。上位バイト自体は
time にほぼ比例（係数およそ 1.77）して 0xbf で頭打ち。尾も素直に伸びる
（-40 dB 到達が 590 → 738 → 1017 → 2076 ms、127 では 3 秒でも届かない）。

### コーラスは別の場所を使う

`40 01 38`（コーラスマクロ）を 0〜7 で振ると、**リバーブのタップは 1 つも動かない**。
動くのは `ram2[29][10]` / `[11]`（変調される遅延タップ）と `ram2[31]` の一群だけ:

| | ram2[29][10] | [11] | ram2[31][0] | [3] | [8] 位相 | [9] | [10] |
|---|---|---|---|---|---|---|---|
| 0 | 15065 | 15013 | 0012 | 0000 | 805e | 004c | 3fb4 |
| 1 | 14850 | 14984 | 00d5 | 0002 | 2f03 | 11d2 | 2e2e |
| 2 | 14930 | 14904 | 0047 | 0004 | b572 | 352b | 0ad5 |
| 3 | 14756 | 14856 | 00b4 | 0008 | 03ac | 3d08 | 02f8 |
| 4 | 15235 | 15213 | 003c | 0020 | abe4 | 2ba8 | 1458 |
| 5 | 15120 | 15138 | 0006 | 0038 | 2aca | 153c | 2ac4 |
| 6 | 16378 | 15100 | 0000 | 0000 | **0000** | 4000 | 0000 |
| 7 | 16378 | 15100 | 0000 | 0028 | **0000** | 4000 | 0000 |

`ram2[31][8]` は位相で、`00:820` 付近のコードが `[9]` と `[10]` をそこから作る
（`0x4000 - 位相` との組で、2 本のタップ用の三角波の対になる）。

**マクロ 6 と 7 だけ位相が 0 で止まっている。** GS のコーラス 6・7 は Short Delay と
Short Delay (FB) — 変調しない遅延で、まさにそのとおりの状態。**変調の仕組みの読みが
これで裏付けられた。**

`ram2[31][0]` は変調の速さ、`[3]` はマクロごとに 0/2/4/8/20/38/0/28 と別値で、
帰還量にあたると思われる（未照合）。

### 鳴り止んだら飛ばす（ビット単位で同一）

エフェクトは回路転写なので、入力がゼロで何も生まないときも同じだけ計算する。
飛ばせる条件を **証明できる形** で決めた。

最初に書いた条件は「入力が 1 周ぶんゼロなら遅延メモリはゼロ」だった。**これは誤り。**
エフェクトには帰還があるので、入力が止まっても尾が回り続け、タップに書かれる値は
ゼロにならない。28 件のビット比較で 17 件が食い違って分かった。

正しい条件は「**直近 1 周のあいだ、遅延メモリへの書き込みがすべてゼロだった**」。
`tv_counter` が 0..0x3fff を巡るあいだに書きタップは全アドレスを覆うので、
そのときメモリ全域がゼロだと言える。ゼロのメモリにゼロを入れれば出力もゼロ。

それでも 4 件残った。コーラスの変調タップを作る**アドレス生成器がゲートの内側**に
あって、飛ばすと LFO の位相が凍り、音が戻ったときに違う所から再開していた。
生成器は遅延メモリに触らないので、ゲートの外へ出した。

```
試験 28 件（リバーブ 8 種、コーラス 8 種、タイム／レベル掃引、実曲 3 曲）
  → 全件ビット単位で同一
```

短縮は控えめ:

```
送りゼロの素材   1.073 秒 → 1.026 秒   4.4%
Hall1            1.076 秒 → 1.036 秒   3.7%
実曲 01HELP      1.818 秒 → 1.804 秒   0.8%
実曲 14DIZZY     1.758 秒 → 1.747 秒   0.6%
```

**プロファイラの `PCM_Update` 自己時間 940 は、エフェクトだけの値ではなかった** —
ボイス演算も出力段も同じ関数に入っている。エフェクト単体の取り分はもっと小さい。
27% という見積もりは過大だった。

### 進め方

ボイスのときと同じ順で:

1. この地図をもとに構成を確定させる（済）
2. A/B 台を作る（`sc55abcompare` と同じ形。同一の入力列から両方を走らせて突き合わせる）
3. 浮動小数で書き直し、段ごとに合わせ込む
