# SC-55 ファームウェアの制御ループ — 実測による特定

対象: `Nuked-SC55-jcmoyer`（JUCE プラグインが実際にビルドしているツリー）
ROM: SC-55 v1.21 / 計測ツール: `tools/sc55probe.cpp`
エミュレートされた H8 のクロック: **20.00 MHz**（実測、1 命令 12 サイクル固定の前提）

---

## 0. 結論

1. **DSP 側は C で書き直されていない。** `src/backend/pcm.cpp` は今も回路をそのまま
   写したサイクル単位のエミュレーションである。だから**発音数 0 でも最大発音時の
   71 % のコストがかかる**。
2. **CPU 側の制御ループは存在し、特定できた。** 予想どおり **1 ms のタイマ割り込み**で
   回っており、**各発音ボイスのピッチ・音量・フィルタ・パン・エフェクト送りを
   250 Hz（4 ms 周期）で PCM チップに書き込んでいる**。
3. その書き込みルーチンは **`cp=00 pc=0x586c–0x5894` の 8 ストア連続**という
   極めて小さいコードで、PCM レジスタに触る命令はファームウェア全体で
   **16 箇所しかない**。

---

## 1. DSP は書き直されていない

`src/backend/pcm.cpp` の `PCM_Update()` は `while (pcm.cycles < cycles)` で
**1 チップサイクルずつ**進み、その中で:

* 16 bit LFSR（`x^0 ^ x^1 ^ x^7 ^ x^12`）を毎サイクル 2 回シフト（`pcm.cpp:592, 614`）
* 20 bit 飽和加算器 `addclip20()` でアキュムレータを更新
* `ram1[30][0..5]` を L/R のアキュムレータとしてピンポンさせる
* スロットを 24 個、**発音していないものも含めて全部**処理する

つまり構造体化・関数分割のリファクタは入っているが、**アルゴリズムは回路の転写のまま**。
「普通の PCM シンセの DSP コード」にはなっていない。

実測（オーディオ 6 秒の生成に要した実時間、同一パッチ、ノートを保持）:

| 同時発音数 | 実時間 | 1 コアに対する負荷 |
|---|---|---|
| 0 | 0.94 秒 | 15.7 % |
| 8 | 1.23 秒 | 20.5 % |
| 16 | 1.33 秒 | 22.2 % |

**発音数 0 で 15.7 %。** 黎明期の PCM 音源としてあり得ない重さの正体はここにある。
本来なら発音数に比例するはずのコストが、ほぼ固定費になっている。

---

## 2. 割り込みの構成（実測）

`MCU_Interrupt_Handle()` をフックして、割り込みが受理されたときの飛び先 PC と
その間隔を記録した。無音時（ノート 0）:

| ハンドラ | 頻度 | 最頻間隔 | 推定 |
|---|---|---|---|
| `cp=00 pc=0379` | **998.8 Hz** | **20000 cycles = 1.000 ms** | **1 ms 制御タイマ** |
| `cp=00 pc=7f20` | **1000.3 Hz** | **20000 cycles = 1.000 ms** | **1 ms 制御タイマ（第2系統）** |
| `cp=00 pc=0450` | 1007 Hz | 6500 cycles = 0.325 ms | バースト的。PCM ボイス IRQ |
| `cp=00 pc=7eb6` | 739 Hz | 6500 cycles = 0.325 ms | 同上 |
| `cp=00 pc=0303` | 157 Hz | 9.92 ms | 約 10 ms 周期 |
| `cp=00 pc=02e3` | 125 Hz | 7.997 ms | **8 ms 周期** |
| `cp=00 pc=0680` | 8.3 Hz | 119.95 ms | 120 ms。LCD／パネル系 |

**1 ms のタイマ割り込みが 2 系統走っている**（`0379` と `7f20`）。
これが「この頃のシンセは 1〜2 ms のタイマで回していた」という読みどおりの構造。

---

## 3. PCM チップに書き込むコードは 16 箇所しかない

PCM レジスタ書き込みを、書き込んだ命令の PC 別に集計した（4 音保持、6 秒）:

### 3.1 ボイスパラメータ更新ルーチン `0x586c – 0x5894`

| PC | 書くレジスタ | PCM 側の意味 |
|---|---|---|
| `586c` | `3e` | **スロット選択**（`pcm.select_channel`） |
| `5894` | `10 11` | `ram2[0]` = **ピッチ増分** |
| `587b` | `12 13` | `ram2[1]` = **パン**（上位 = L、下位 = R） |
| `5880` | `14 15` | `ram2[2]` = **エフェクト送り**（上位 = リバーブ、下位 = コーラス） |
| `5876` | `16 17` | `ram2[3]` = **TVA エンベロープ** `{目標, 速度}` |
| `5871` | `18 19` | `ram2[4]` = **TVF エンベロープ** |
| `5885` | `1a 1b` | `ram2[5]` = **第3エンベロープ**（カットオフ） |
| `588f` | `1c 1d` | `ram2[6]` = **フラグ**（bit0 = IRQ 許可、bit1 = 出力タップ） |

レジスタ→`ram2` の対応は `pcm.cpp:180` の `ix = (address >> 1) & 7`（`address & 32` で `ix |= 8`）。
各フィールドの意味は DSP 側の使われ方から確定できる:
`ram2[1]` パン → `pcm.cpp:1465`、`ram2[2]` 送り → `1466`、`ram2[11]` フィルタ係数 → `1375`、
`ram2[7] & 0x20` キーオン → `1106`、`ram2[0]` ピッチ加算 → `1159`。

**これが探していたルーチン。** スロットを選んで、そのボイスの
ピッチ・パン・送り・TVA・TVF・カットオフ・フラグを 7 ワード続けて書く、それだけ。

### 3.2 エフェクトスロット更新ルーチン `0x31a9 – 0x3212`

| PC | レジスタ | 意味 |
|---|---|---|
| `31a9` | `3e` | スロット選択 |
| `31b5` | `16 17` | `ram2[3]` |
| `31d3` | `18 19` | `ram2[4]` |
| `3200` | `1a 1b` | `ram2[5]` |
| `31e5` | `34 35` | `ram2[10]` |
| `3212` | `36 37` | `ram2[11]` |

`0x34–0x37` は `ix |= 8` されるので `ram2[10]/[11]`。スロット 28〜31 は
ボイスではなくリバーブ／コーラス／ミキサの状態領域なので、これはエフェクト側の更新。

### 3.3 ノートオン時 `0x29d7 – 0x29d9`

`3e`（スロット選択）→ `10 11`（ピッチ）。発音開始のときだけ数回。

---

## 4. 制御レートは 250 Hz / ボイス（1 ms タイマの 4 回に 1 回）

同じパッチで発音数だけ変えて、`0x5871` の実行回数を測った（6 秒）:

| 同時発音数 | `0x5871` 実行回数 | 1 秒あたり | 1 ボイスあたり | PCM 書き込み総数 |
|---|---|---|---|---|
| 0 | 0 | 0 | — | **0** |
| 1 | 1,498 | 249.7 Hz | **249.7 Hz** | 2,747 /秒 |
| 4 | 5,994 | 999.0 Hz | **249.8 Hz** | 10,988 /秒 |
| 8 | 11,998 | 1999.7 Hz | **250.0 Hz** | 21,997 /秒 |

**完全に線形。1 ボイスあたり 250 Hz = 4 ms 周期。**
1 ms タイマが 1000 Hz なので、**各ティックで発音中ボイスの 1/4 を更新している**
（8 音なら 1 ティックあたり 2 ボイス）。発音数 0 なら書き込みは **完全にゼロ**。

1 ボイス 1 回の更新で 11 ワード書く（2747 ÷ 250）。

書き込みがどの割り込みの中で起きているか（4 音）:

| ハンドラ | 書き込みの割合 |
|---|---|
| `pc=7f20`（1 ms） | 40.4 % |
| `pc=0379`（1 ms） | 26.9 % |
| `pc=0450`（PCM IRQ） | 22.6 % |
| `pc=02e3`（8 ms） | 9.2 % |

→ **書き込みの 67 % は 1 ms タイマ割り込みの中**。残りは PCM のボイス IRQ
（エンベロープが目標に到達したときの次セグメント書き込み）と 8 ms 周期の処理。

---

## 5. これが意味すること

ファームウェアの「バックエンド」は、実体としては次の 3 つしかない:

1. **1 ms タイマ割り込み**（`0379` / `7f20`）— 発音中ボイスを 4 分割し、
   各ボイスのピッチ・音量・フィルタ・パン・送りを計算して 7 ワード書く（250 Hz/voice）
2. **PCM ボイス IRQ**（`0450` / `7eb6`）— エンベロープのセグメント遷移とループ処理
3. **8 ms / 10 ms 周期**（`02e3` / `0303`）— LFO・ポルタメント等の低レート更新

出力インタフェースは `ram1[32][8]` と `ram2[32][16]`、書き込み命令は 16 箇所。
**この 3 つを再現すれば H8 の実行は不要になる。**

そして重要なのは、**エンベロープ本体もループ処理もチップ側（`calc_tv`, アドレス比較器）に
あって、ファームウェアは目標値と速度を書くだけ**という点。制御レートで
やっている計算は素朴なもので、1 ms ごとに数十ボイス分のテーブル参照と加減算にすぎない。
実機の H8/532（20 MHz、1 命令 12 サイクル ≒ 1.67 MIPS）で回っていたのだから、
現代の CPU でネイティブに書けば実質ゼロコストになる。

---

## 6. 次にやること（優先順）

| | 内容 | 効果 |
|---|---|---|
| **A** | `PCM_Update()` で非発音スロットを早期スキップ | 無音時のコストがほぼ消える（今 15.7 %） |
| **B** | `TIMER_Clock()` をイベント駆動化（今も 1 サイクルずつ FRT 3 個 + TMR を評価、`mcu_timer.cpp:379`） | 最大の固定費が消える |
| **C** | `MCU_Step()` の SLEEP 中を早送り | 無音時の CPU 実行が消える |
| **D** | `0x586c` ルーチンと 1 ms 割り込みをネイティブ実装（トレース差分駆動） | H8 実行そのものが不要に |
| **E** | `PCM_Update()` を浮動小数点の普通の DSP に書き換え | 残りのコストが 1 桁減る。ただし音は変わる |

A〜C は解析不要で、いま audio スレッド上で走っている以上、
バッファ締め切りに対する負荷に直結する。D の前にやるべき。

---

## 付録: 計測の再現

`tools/sc55probe.cpp` は `PCM_Write` と `MCU_Interrupt_Handle` をマクロで
差し替えてフックする:

```sh
J=.   # このリポジトリのルート
mkdir -p /tmp/gen && cat > /tmp/gen/config.h <<'EOF'
#pragma once
#define NUKED_ENABLE_ASIO 0
#define NUKED_VERSION "probe"
#define NUKED_SOURCE "probe"
#define NUKED_VERSION_MAJOR 0
#define NUKED_VERSION_MINOR 0
#define NUKED_VERSION_PATCH 0
#include <cstdio>
void Cfg_WriteVersionInfo(FILE* file);
EOF

clang++ -std=c++20 -O2 -c -DPCM_Write=PCM_Write_Impl \
  -I/tmp/gen -Isrc/backend -Isrc/common -IPlugins/Source src/backend/pcm.cpp -o /tmp/pcm.o
clang++ -std=c++20 -O2 -c -DMCU_Interrupt_Handle=MCU_Interrupt_Handle_Impl \
  -I/tmp/gen -Isrc/backend -Isrc/common -IPlugins/Source src/backend/mcu_interrupt.cpp -o /tmp/int.o
clang -std=c11 -O2 -Isrc/backend/sha -c src/backend/sha/sha224-256.c -o /tmp/sha.o

clang++ -std=c++20 -O2 -I/tmp/gen -Isrc/backend -Isrc/common -IPlugins/Source \
  tools/sc55probe.cpp /tmp/pcm.o /tmp/int.o /tmp/sha.o \
  Plugins/Source/NukedSC55Emulator.cpp \
  src/backend/{mcu,mcu_opcodes,mcu_timer,submcu,lcd,emu,rom,rom_io,file_io,file_hashing,config,diagnostics,standard_romsets}.cpp \
  src/common/{rom_loader,path_util}.cpp -o tools/sc55probe

./tools/sc55probe 0 6     # 無音
./tools/sc55probe 8 6     # 8 音保持
```

第1引数 = 保持するノート数、第2引数 = 計測秒数。ROM ディレクトリはソース冒頭で指定。
