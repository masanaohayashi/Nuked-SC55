# 第2フィルター補正テーブル

TryStepSecondFilterCurveでSC-55 v1.21の42bf..4322、40命令を単命令置換。
第1経路との差分はパッチbyte58/60、ページ3のdd12テーブル、保存先voice[-42]。
さらに4313のAND.W ffが最終テーブル参照前に入り、R3上位byteを消す。
分岐4304はこのANDへ接続する。後続PCの4バイト差も反映。

ページ切り替え、符号補正、byte乗算、倍化、SWAP、テーブル変換を再現。
ゼロ補正時は256を直接保存する。
EP=0のROM/SRAM、EP=1〜4のROMからのパッチ参照に対応。
dd12/67c6は偶数インデックス0..510、679aは0..255を対象とする。
割り込み境界を維持し、動的確保・ロック・UI呼び出しは追加しない。

- 全40命令×384＝15,360境界でPC/SR/全レジスタ/DP/EP/ex_ignore/SRAM一致。
- 379,676ステレオフレームがビット一致。
- 同じ再生のH8 fallbackは2,854,715→2,853,938。
- Mac Release Shared Codeビルド成功（署名なし）。

CPU改善量、Logicでの動作は未測定。4327以降は
[コントローラー係数](NATIVE_FILTER_CONTROLLER_SCALE_2026-09-09.md)で置換済み。
