# 第2フィルターコントローラー係数

TryStepSecondFilterControllerScaleでSC-55 v1.21の437c..43ce、32命令を単命令置換。
第1経路と命令列は共通で、パッチbyte63を読み、voice[-38]へ保存する。
PCと分岐先は第1経路から55だけ後方。
負側の乗算、正側の除算、4/ffffへの制限、ゼロ補正時の256を再現する。
DIVXUの商オーバーフロー時はレジスタを維持してVを設定。ゼロ除算はH8へ戻す。

EP=0のROM/SRAM、EP=1〜4のROMからのパッチ参照に対応。
679aテーブルの参照条件はR3=0..255。割り込み境界を維持し、
動的確保・ロック・UI呼び出しは追加しない。

- 全32命令×384＝12,288境界でPC/SR/全レジスタ/DP/EP/ex_ignore/SRAM一致。
- 379,676ステレオフレームがビット一致。
- 同じ再生のH8 fallbackは2,853,738→2,853,288。
- Mac Release Shared Codeビルド成功（署名なし）。

CPU改善量、Logicでの動作は未測定。43d1以降は
[設定転送・初期化](NATIVE_FILTER_SETUP_2026-09-09.md)で置換済み。
