# 第5フィルター深度補正

TryStepFifthFilterDepthでSC-55 v1.21の410d..4189、56命令を単命令置換。
第4深度と命令列は共通。パッチbyte49から深度を読み、voice[94]へ保存する。
PCと分岐先は第4経路から7fだけ後方。voice[-36]の係数との乗算、
79f2テーブル参照、桁上げ、符号処理と7fff/8001への飽和を再現。

EP=0のROM/SRAM、EP=1〜4のROMからのパッチ参照に対応。
79f2テーブルの参照条件はR3=0..255。ガード外はH8へ戻す。
割り込み境界を維持し、動的確保・ロック・UI呼び出しは追加しない。

- 全56命令×384＝21,504境界でPC/SR/全レジスタ/DP/EP/ex_ignore/SRAM一致。
- 379,676ステレオフレームがビット一致。
- 同じ再生のH8 fallbackは2,857,342→2,856,767。
- Mac Release Shared Codeビルド成功（署名なし）。

CPU改善量、Logicでの動作は未測定。418c以降は
[最大値選択](NATIVE_FILTER_MAXIMUM_2026-09-09.md)で置換を継続。
